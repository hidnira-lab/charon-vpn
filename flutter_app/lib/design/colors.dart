import 'package:flutter/widgets.dart';

/// Color tokens from `needs/ui-ux-reference` ("mecha technical console").
/// Dark mode only for now — light mode is out of scope for the MVP redesign.
class CharonColors {
  CharonColors._();

  static const background = Color(0xFF0B0E14);
  static const surface = Color(0xFF1F2937);
  static const surface2 = Color(0xFF161D29);
  static const foreground = Color(0xFFE8EAED);
  static const muted = Color(0xFF8A95A6);
  static const primary = Color(0xFF1D4ED8);
  static const primaryBright = Color(0xFF3B82F6);
  static const critical = Color(0xFFDC2626);
  static const caution = Color(0xFFF5B301);
  static const steel = Color(0xFF374151);
}
