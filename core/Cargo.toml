[package]
name = "charon-core"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
cidr = "0.3"
tun2proxy = "0.8"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time"] }
log = "0.4"
